const User = require('./User')
const Trips = require('./Trips')
const Cities = require('./Cities')
const Markers = require('./Markers')
const TripsCities = require('./TripsCities')

module.exports = {
  User, Trips, Cities, Markers, TripsCities
}
